def sumDiagonal(a):
    sum=0
    for i in range(0,len(a)):
        for j in range(0,len(a)):
            if i==j:
                sum=sum+a[i][j]
    return sum
    
    
    or
    
    
    def sumDiagonal(a):
    sum = 0
    for i in range(len(a)):
        sum += a[i][i]
    return sum
